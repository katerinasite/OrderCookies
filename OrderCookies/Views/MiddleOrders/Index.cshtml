@model IEnumerable<OrderCookies.Models.MiddleOrder>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cookies.CookiesName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FinalOrder.FinalOrderId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Number)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MiddleAmount)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cookies.CookiesName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FinalOrder.FinalOrderId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Number)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MiddleAmount)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MiddleOrderId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MiddleOrderId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MiddleOrderId })
        </td>
    </tr>
}

</table>
