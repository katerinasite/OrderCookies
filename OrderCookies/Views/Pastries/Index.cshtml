@model IEnumerable<OrderCookies.Models.Pastry>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PastryName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PastryName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PastryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PastryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PastryId })
        </td>
    </tr>
}

</table>
