@model IEnumerable<OrderCookies.Models.Filling>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FillingName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FillingName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FillingId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FillingId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FillingId })
        </td>
    </tr>
}

</table>
