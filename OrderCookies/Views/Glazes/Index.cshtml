@model IEnumerable<OrderCookies.Models.Glaze>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GlazeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GlazeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GlazeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GlazeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GlazeId })
        </td>
    </tr>
}

</table>
